# Repository instructions

- After making changes to the browser extension, increment the patch version in `firefox-extension/manifest.json` before finishing the task, unless the user specifies a different version.
